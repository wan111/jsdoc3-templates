<?js
/*
* Returns:  
   * {type} : description
*/

var self = this;
var captions = self.captions;
var println = self.println(print, "   ");

println("* " + captions.returns + "   ", indent);
data.forEach(function(ret){
	print("\n");
	println("* {" + ret.type.names.join("|") + "}" + (ret.description ? " : " + ret.description : ""), indent + 1);
});
?>