<?js
/*
* Example:  

   ```
   code
   ```
*/
var self = this;
var captions = self.captions;
var println = self.println(print, "   ");

println("* " + captions.examples + "  ", indent);
data.forEach(function(example){
	print("\n");
	println("```", indent + 1);
	println(example, indent + 1);
	println("```  ", indent + 1);
});

?>